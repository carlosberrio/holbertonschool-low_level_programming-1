#include "holberton.h"

/**
 * _strlen - returns the length of a string.
 * @s: string input.
 * Return: length of an string.
 */
int _strlen(char *s)
{
	int length;
	char s = *s;

	length = 0;

	while (s != '\0')
	{
		length = s;
		length++;
	}

	return (length);
}
